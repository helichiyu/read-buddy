# TODO: Prompt 自检 + 工具模块化重构 - 2026/06/02

## 需求描述

当前 AI 在对话中经常只说"已记录""已推荐"，但实际没有调用 Function Calling 工具函数，导致数据没有真正存入数据库。

**解决方案**：
1. **Prompt 自检**：在 System Prompt 中加入"回复后自检"指令，让 AI 生成回复后自动检查是否需要调用工具。保持单轮调用 + `tool_choice: "auto"`，不增加额外 API 请求。
2. **工具模块化（Skill 化）**：将当前集中在 `ai_service.py` 中的工具定义和 `app.py` 中的处理逻辑拆分为独立的工具模块，结构清晰、易维护。

**约束**：继续支持所有 OpenAI 兼容 API（DeepSeek、OpenAI、Claude 等）。

⚠️ **前置问题**：当前 `app.py` 第 230 行存在 SyntaxError，应用完全无法启动。需要先修复才能进行后续开发。

---

## 实施计划

### 前置修复：恢复应用可运行状态

当前 `app.py` 第 230 行的 `return` 和 `await` 写在了同一行，导致 SyntaxError：

```python
# 第 230 行（当前，有语法错误）：
return {"ok": True, "message": f"已记录《{title}》的评价：{stars}星"} await db.add_book(...)

# 第 231-233 行是多余的死代码：
await db.add_rating(book_id, stars, review)
books_changed.append({"action": "rated", "title": title, "stars": stars})
return {"ok": True, "message": f"已记录《{title}》的评价：{stars}星"}
```

**修复方式**：将第 230 行改为正常的 return 语句，删除 231-233 行：

```python
# 修改后第 230 行：
        return {"ok": True, "message": f"已记录《{title}》的评价：{stars}星"}
        # 删除原来的 231-233 行
```

### 阶段 1：工具模块化重构（Skill 化）

#### 1.1 创建工具目录结构

```
backend/
├── tools/
│   ├── __init__.py          # 工具注册器（自动加载所有工具）
│   ├── base.py              # 工具基类
│   ├── rate_book.py         # 评价书籍
│   ├── recommend_books.py   # 推荐书籍
│   ├── accept_book.py       # 接受推荐
│   ├── reject_book.py       # 拒绝推荐
│   ├── discuss_book.py      # 聊书
│   └── save_preference.py   # 保存偏好
```

#### 1.2 工具基类

创建 `backend/tools/base.py`：

```python
"""工具基类 - 所有工具必须继承并实现 schema 和 handler"""

from abc import ABC, abstractmethod


class BaseTool(ABC):
    """工具基类"""

    @property
    @abstractmethod
    def name(self) -> str:
        ...

    @property
    @abstractmethod
    def description(self) -> str:
        ...

    @property
    @abstractmethod
    def parameters(self) -> dict:
        ...

    def to_openai_tool(self) -> dict:
        """转换为 OpenAI Function Calling 格式"""
        return {
            "type": "function",
            "function": {
                "name": self.name,
                "description": self.description,
                "parameters": {
                    "type": "object",
                    "properties": self.parameters,
                    "required": self._required_params,
                },
            },
        }

    @property
    def _required_params(self) -> list[str]:
        return []

    @abstractmethod
    async def handle(self, args: dict, context: dict) -> dict:
        """
        执行工具逻辑

        Args:
            args: AI 传来的参数（已解析为 dict）
            context: 上下文，包含 books_changed 列表

        Returns:
            dict: 返回给 AI 的结果
        """
        ...
```

#### 1.3 工具注册器

创建 `backend/tools/__init__.py`：

```python
"""工具注册器 - 自动发现和加载所有工具模块"""

import importlib
import pkgutil
from typing import Dict
from .base import BaseTool

_registry: Dict[str, BaseTool] = {}


def _auto_discover():
    """自动发现 tools/ 目录下的所有工具模块"""
    import tools as pkg
    for _, name, _ in pkgutil.iter_modules(pkg.__path__):
        if name.startswith("_") or name == "base":
            continue
        module = importlib.import_module(f"tools.{name}")
        for attr_name in dir(module):
            attr = getattr(module, attr_name)
            if (isinstance(attr, type)
                    and issubclass(attr, BaseTool)
                    and attr is not BaseTool):
                instance = attr()
                _registry[instance.name] = instance


def get_all_tools() -> list[BaseTool]:
    if not _registry:
        _auto_discover()
    return list(_registry.values())


def get_tool(name: str) -> BaseTool | None:
    if not _registry:
        _auto_discover()
    return _registry.get(name)


def get_openai_tools() -> list[dict]:
    return [t.to_openai_tool() for t in get_all_tools()]
```

#### 1.4 各工具模块

每个工具遵循相同模式，从 `app.py` 的 `_handle_tool_call` 中迁移逻辑。

以 `rate_book.py` 为例：

```python
"""评价书籍工具"""

import database as db
from .base import BaseTool


class RateBookTool(BaseTool):
    @property
    def name(self) -> str:
        return "rate_book"

    @property
    def description(self) -> str:
        return "用户对一本书做出了评价（包含星级和/或文字评价）"

    @property
    def parameters(self) -> dict:
        return {
            "book_title": {"type": "string", "description": "书名"},
            "author": {"type": "string", "description": "作者"},
            "stars": {"type": "integer", "description": "星级评分 1-5"},
            "review": {"type": "string", "description": "用户的文字评价"},
            "series_name": {"type": "string", "description": "系列名称，单本不填"},
            "series_index": {"type": "integer", "description": "系列序号，单本不填"},
        }

    @property
    def _required_params(self) -> list[str]:
        return ["book_title", "stars"]

    async def handle(self, args: dict, context: dict) -> dict:
        title = args.get("book_title", "")
        stars = args.get("stars", 0)
        review = args.get("review", "")
        author = args.get("author", "")
        series_name = args.get("series_name", "")
        series_index = args.get("series_index", 0)

        # 查找是否已有同名书籍
        all_books = await db.get_all_books()
        existing = None
        for b in all_books:
            if b["title"] == title and b.get("series_index", 0) == series_index:
                existing = b
                break

        if existing:
            book_id = existing["id"]
            await db.update_book_status(book_id, "rated")
            if author and not existing.get("author"):
                await db.update_book_details(book_id, author=author)
            if series_name and not existing.get("series_name"):
                await db.update_book_details(
                    book_id, series_name=series_name, series_index=series_index
                )
        else:
            book_id = await db.add_book(
                title=title, author=author, status="rated",
                series_name=series_name, series_index=series_index,
            )

        # 尝试获取书籍详情
        try:
            from book_service import search as search_book
            query = f"{title} {author}".strip()
            book_info = await search_book(query)
            if book_info:
                await db.update_book_details(
                    book_id,
                    cover_url=book_info.get("cover_url", ""),
                    description=book_info.get("description", ""),
                    isbn=book_info.get("isbn", ""),
                    categories=book_info.get("categories", ""),
                    author=book_info.get("author", "") or author,
                )
        except Exception:
            pass

        await db.add_rating(book_id, stars, review)
        book = await db.get_book(book_id)
        context["books_changed"].append({"action": "rated", **book})
        return {"ok": True, "message": f"已记录《{title}》的评价：{stars}星"}
```

其余工具按同样模式迁移：
- `recommend_books.py`：从 `_handle_tool_call` 的 `recommend_books` 分支迁移（`app.py` 第 235-246 行）
- `accept_book.py`：从 `accept_book` 分支迁移（第 248-273 行）
- `reject_book.py`：从 `reject_book` 分支迁移（第 275-294 行）
- `discuss_book.py`：从 `discuss_book` 分支迁移（第 296-300 行）
- `save_preference.py`：从 `save_preference` 分支迁移（第 302-309 行）

---

### 阶段 2：Prompt 自检机制

#### 2.1 修改 System Prompt

修改 `backend/ai_service.py` 中的 `build_system_prompt()`，在 **Prompt 最前面** 加入工具自检指令：

```python
async def build_system_prompt() -> str:
    # ... 获取 profile、偏好、书籍摘要等（逻辑不变）

    prompt = f"""你是 {buddy_name}，一个友好热情的阅读伙伴。
{f"称呼用户为「{user_name}」。" if user_name else ""}

## ⚠️ 工具调用自检（每次回复必须遵守）

每次生成回复后，你必须自问：我的回复是否涉及以下操作？

| 如果你说/做了... | 必须调用的工具 |
|---|---|
| 确认了用户的评价、打分 | `rate_book` |
| 推荐了具体书籍 | `recommend_books` |
| 确认用户接受了推荐 | `accept_book` |
| 记录用户不想看某本书 | `reject_book` |
| 开始聊某本书的背景/作者 | `discuss_book` |
| 确认记住用户的偏好要求 | `save_preference` |

- 如果涉及任何操作 → **必须调用对应工具**，不能只在文字里说"已记录"
- 如果只是纯聊天（问候、追问、讨论）→ 不调用工具，完全正常
- 可以同时调用多个工具（比如推荐书的同时保存偏好）

## 绝对规则（不可违反）

### 禁止剧透
...（后续内容不变）

## 核心规则
...（不变）

## 上下文信息
...（不变）

## 注意
- 用中文回复
- 友好、有耐心、像一个真正的朋友在聊天
"""
    return prompt
```

**改动要点**：
- 把工具自检放在 System Prompt 的 **最前面**（仅次于身份介绍）
- 用表格形式让映射关系一目了然
- 明确说"不能只在文字里说已记录"
- 纯聊天不需要调工具，避免过度调用

#### 2.2 修改 app.py 的 /api/chat

将 `_handle_tool_call` 替换为工具注册器分发：

```python
# app.py 改动点

# 旧代码（删除）：
# from ai_service import get_ai_service, build_system_prompt, TOOLS
# 旧代码（删除）：_handle_tool_call 函数

# 新代码：
from ai_service import get_ai_service, build_system_prompt
from tools import get_openai_tools, get_tool

@app.post("/api/chat")
async def chat(data: dict = Body(...)):
    # ... 前面不变（获取 user_content、检查 AI 配置等）

    # 构建上下文（不变）
    system_prompt = await build_system_prompt()
    recent_messages = await db.get_recent_messages(limit=20)
    chat_messages = [
        {"role": m["role"], "content": m["content"]}
        for m in recent_messages
    ]

    # 使用注册器的工具列表
    tools = get_openai_tools()

    # 调用 AI（可能多轮处理工具调用）
    books_changed = []
    total_tokens = 0
    max_rounds = 5

    for _ in range(max_rounds):
        message, tokens = await ai.chat(chat_messages, system_prompt, tools=tools)
        total_tokens += tokens

        if not message.tool_calls:
            break

        # 将 AI 回复加入上下文
        chat_messages.append({
            "role": "assistant",
            "content": message.content or "",
            "tool_calls": [
                {
                    "id": tc.id,
                    "type": "function",
                    "function": {
                        "name": tc.function.name,
                        "arguments": tc.function.arguments,
                    },
                }
                for tc in message.tool_calls
            ],
        })

        # 使用注册器分发工具调用
        for tc in message.tool_calls:
            import json as _json
            args = _json.loads(tc.function.arguments)
            tool = get_tool(tc.function.name)
            if tool:
                result = await tool.handle(args, {"books_changed": books_changed})
            else:
                result = {"ok": False, "message": f"未知工具：{tc.function.name}"}
            chat_messages.append({
                "role": "tool",
                "tool_call_id": tc.id,
                "content": _json.dumps(result, ensure_ascii=False),
            })

    # 存储和返回（不变）
    reply_text = message.content or ""
    await db.add_message("assistant", reply_text, total_tokens)
    await db.add_token_usage(total_tokens)

    return {
        "reply": reply_text,
        "tokens": total_tokens,
        "books_changed": books_changed,
    }
```

---

### 阶段 3：清理

- [ ] 删除 `ai_service.py` 中的 `TOOLS` 列表（约 120 行）
- [ ] 删除 `app.py` 中的 `_handle_tool_call` 函数（约 130 行）
- [ ] 更新 `app.py` 的 import

---

## 任务清单

### 前置修复：恢复应用可运行状态
- [ ] 修复 `app.py` 第 230 行 SyntaxError（`return` 和 `await` 同行）
- [ ] 删除第 231-233 行死代码
- [ ] 验证 `python -c "import app"` 不再报错

### 阶段 1：工具模块化重构
- [ ] 创建 `backend/tools/` 目录
- [ ] 编写 `backend/tools/base.py` 工具基类
- [ ] 编写 `backend/tools/__init__.py` 工具注册器
- [ ] 编写 `backend/tools/rate_book.py`（从 `app.py:187-233` 迁移）
- [ ] 编写 `backend/tools/recommend_books.py`（从 `app.py:235-246` 迁移）
- [ ] 编写 `backend/tools/accept_book.py`（从 `app.py:248-273` 迁移）
- [ ] 编写 `backend/tools/reject_book.py`（从 `app.py:275-294` 迁移）
- [ ] 编写 `backend/tools/discuss_book.py`（从 `app.py:296-300` 迁移）
- [ ] 编写 `backend/tools/save_preference.py`（从 `app.py:302-309` 迁移）

### 阶段 2：Prompt 自检 + 路由更新
- [ ] 修改 `ai_service.py` 的 `build_system_prompt()`，加入工具自检表格
- [ ] 修改 `app.py` 的 `/api/chat`，使用工具注册器分发
- [ ] 删除 `ai_service.py` 的 `TOOLS` 列表
- [ ] 删除 `app.py` 的 `_handle_tool_call` 函数

### 阶段 3：清理和验证
- [ ] 端到端测试

---

## 验证标准

### 阶段 1 验证
- [ ] `from tools import get_openai_tools` 返回 6 个工具
- [ ] 每个工具的 `to_openai_tool()` 输出与原 `TOOLS` 中对应项格式一致
- [ ] 各工具 `handle()` 逻辑与原 `_handle_tool_call` 中对应分支行为一致

### 阶段 2 验证
- [ ] AI 评价书籍时调用 `rate_book`，数据正确存入数据库
- [ ] AI 推荐书时调用 `recommend_books`，推荐展示在聊天区
- [ ] 用户接受推荐时调用 `accept_book`，书籍出现在右侧书架
- [ ] 用户拒绝推荐时调用 `reject_book`，记录原因
- [ ] 用户点击书架卡片时触发 `discuss_book`
- [ ] 用户表达偏好时调用 `save_preference`
- [ ] 纯聊天（问候、追问）时 AI 不调用任何工具
- [ ] 多工具同时调用正常（如推荐 + 保存偏好）
- [ ] 前端无需任何改动（接口返回格式不变）
- [ ] Token 统计正确

### 阶段 3 验证
- [ ] `ai_service.py` 不包含 `TOOLS` 列表
- [ ] `app.py` 不包含 `_handle_tool_call` 函数
- [ ] 完整流程：首次打开 → 聊天 → 评价 → 推荐 → 关闭 → 重新打开，均正常

---

## 后续优化（本次不实施）

如果 Prompt 自检方案效果不够好，可以升级为 **状态机 + 动态 `tool_choice`** 方案：

### 思路

后端维护一个对话状态机，根据当前状态决定传给 AI 的工具列表和 `tool_choice` 参数：

```
状态流转示例：

[空闲] → 用户说了评价 → [待记录评价]
                         ↓
                    只发 rate_book 工具 + tool_choice="required"
                         ↓
                    AI 必须调 rate_book → [空闲]

[空闲] → AI 要推荐书 → [推荐中]
                         ↓
                    只发 recommend_books + tool_choice="required"
                         ↓
                    AI 必须调 recommend_books → [空闲]

[空闲] → 纯聊天 → 不发工具，tool_choice 不设置
```

### 优势

- **最可靠**：AI 没有选择余地，只能调用指定的工具
- **省 token**：每次只发 1-2 个工具定义，不发的工具 AI 看不到
- **不依赖 Prompt 质量**：即使 AI 模型工具调用能力弱，也能保证执行

### 何时升级

当以下情况出现时考虑升级：
- 多个用户反馈 AI 仍然不调工具
- 切换了工具调用能力较弱的模型（如某些开源模型）
- 需要更严格的数据一致性保障
